# MangaKun 設定

image:
  # Gemini 画像モデル（2026-09 時点の候補）
  #   gemini-3.1-flash-image      … 標準。2K で約 $0.101/枚
  #   gemini-3-pro-image          … 高品質。2K で約 $0.134/枚
  #   gemini-3.1-flash-lite-image … 安価。1K のみ・約 $0.034/枚
  model: gemini-3.1-flash-image
  # 0.5K / 1K / 2K / 4K（Kindle 1600x2560 のページに配置するため 2K を既定にする）
  size: 2K
  # 既定のアスペクト比（1:1, 3:2, 2:3, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9）
  aspect: "3:4"
  # 1枚あたりの料金（米ドル、2026-09 時点）。費用の報告と上限チェックに使う
  prices_usd:
    gemini-3.1-flash-image: {0.5K: 0.045, 1K: 0.067, 2K: 0.101, 4K: 0.151}
    gemini-3-pro-image: {1K: 0.134, 2K: 0.134, 4K: 0.24}
    gemini-3.1-flash-lite-image: {1K: 0.0336}
  usd_to_jpy: 150   # 円換算の目安
  parallel: 4       # 同時に生成する枚数

# カラーが既定。false でモノクロ（スクリーントーン風）
color: true

# 生成枚数の上限（費用の暴走防止）
retake_limit_per_panel: 2      # 自己チェックで NG のコマを自動で作り直す回数
max_images_per_page: 8         # 作品全体の上限 = ページ数 × この値 + キャラ数 × 2 + 表紙 2

# ページ合成（compose_page.py）
page:
  size: [1600, 2560]     # Kindle 推奨 1:1.6
  margin: [0.05, 0.04]   # 外周の余白（幅・高さに対する割合）
  gutter: [22, 44]       # コマ間の白（px）。左右の間 / 上下の間（漫画は上下を広めにする）
  border: 5              # コマ枠線の太さ（px）
  font_size: 44          # セリフの文字サイズ（px）
  max_chars: 10          # 1列の最大文字数（改行を書かなかったときの自動折り返し）
  jpeg_quality: 92
