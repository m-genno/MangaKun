# MangaKun 漫画作成スキル 計画書

作成日: 2026-09-23

## 1. 概要

作業フォルダ内で Claude Code に「以下の漫画を作って」と伝えると、漫画を作成するスキル。
できあがったものに対して、同一セッション内で修正指示を出して完成させる。

- 目的: Kindle（KDP）での出版
- 利用者: 本人（ITエンジニア、セットアップ担当）とパートナー（PC・AIに不慣れ）
- 動作環境: Windows 11（本PC） / MacBook Air
- 一般配布はしない（非公開 GitHub リポジトリで管理）

### 入力情報

| 項目 | 必須 | 備考 |
|---|---|---|
| ストーリー | ○ | 数行のあらすじでも可 |
| ページ数 | ○ | 表紙は別 |
| 登場人物 | ○ | 名前・性格・見た目など |
| テイスト（画風） | △ | 省略時はプリセットから提案 |
| カラー / モノクロ | － | **デフォルトはカラー** |
| モード | － | 確認モード（デフォルト） / おまかせモード |

## 2. 決定事項

| 項目 | 決定 |
|---|---|
| 画像生成 | **Google Gemini API** `gemini-3.1-flash-image`（2K）。モデル名は `config.yaml` で切り替え可能 |
| APIキー | 本人のキーを2台で共有。各PCの `.env` に手動で配置（Git管理しない） |
| スキルの管理・配布 | **非公開 GitHub リポジトリ** |
| 色 | **カラーがデフォルト**。指示があればモノクロ（スクリーントーン風） |
| モード | **確認モード**（デフォルト）と **おまかせモード**（最後まで自動） |
| 綴じ方向 | 右綴じ（日本の漫画形式）、セリフは縦書き |

## 3. 基本方針

Claude Code 自体は画像を描けないため、役割を分担する。

| 役割 | 担当 |
|---|---|
| ストーリー → ネーム（コマ割り・構図・セリフ）作成 | Claude |
| 各コマの絵を描く | Gemini 画像モデル |
| コマ配置・吹き出し・縦書きセリフの合成 | Python スクリプト |
| 出来上がりのチェック（顔が吹き出しで隠れていないか、キャラが別人になっていないか等） | Claude が画像を見て確認 |
| Kindle 用ファイルへの書き出し | Python スクリプト |

### コマ単位で生成してスクリプトで組み立てる

ページ全体を1枚で生成せず、コマごとに生成して合成する。

- AI は絵の中の日本語をよく崩すため、セリフは絵に描かせず、後からフォントで入れる
- セリフ修正に API 費用がかからず、すぐ終わる（合成し直すだけ）
- 「3ページ目の2コマ目だけ直して」のように、1コマ単位でやり直せる
- キャラ設定画を参照画像として毎回渡し、ページが変わっても顔を揃える
- 作業状態はすべて `manga.yaml` に保存するため、セッションが切れても続きから作業できる
- コマ生成時は「吹き出し用に◯◯側に余白を空ける」ようにプロンプトで指示する
  - 「上部を空けて」とだけ書くと、枠線付きの空白コマを描いてしまうことがある（試験で発生）。「1枚の連続した絵・枠線なし・上の1/3は空や壁など同じ場面の続き」と書くと防げる

## 4. フォルダ構成

GitHub で管理するのはスキル本体のみ。作品フォルダと `.env` は `.gitignore` で除外する。

```
MangaKun/                          ← 作業フォルダ = Git リポジトリ
├─ .gitignore                      ← スキル本体以外（作品フォルダ・.env）を除外
├─ .env.example                    ← GEMINI_API_KEY=xxxx の雛形
├─ .env                            ← 実際の API キー（Git 管理外）
├─ README.md                       ← セットアップ手順
├─ PLAN.md                         ← 本ファイル
├─ .claude/skills/manga/
│   ├─ SKILL.md                    ← 作業手順・モード・修正ルール
│   ├─ config.yaml                 ← モデル名、既定の色、リテイク上限、生成枚数上限など
│   ├─ scripts/
│   │   ├─ gen_image.py            ← Gemini API 呼び出し（参照画像・アスペクト比指定）
│   │   ├─ compose_page.py         ← コマ配置・ページ合成（manga.yaml → pages/pNN.jpg）
│   │   ├─ lettering.py            ← 縦書きセリフ・吹き出し・効果音の描画（compose / export から使う）
│   │   ├─ export_kindle.py        ← EPUB（固定レイアウト・右綴じ）/ PDF / 表紙の書き出し
│   │   └─ open_file.py            ← 完成画像を OS 標準ビューアで開く（Win: start / Mac: open）
│   ├─ layouts/                    ← コマ割りテンプレート（1〜6コマ、見開き用など）
│   ├─ styles.md                   ← 画風プリセット（少女漫画、少年漫画、劇画、ほのぼの、絵本風…）
│   └─ fonts/                      ← 源暎アンチック（セリフ）、Dela Gothic One（叫び・効果音・タイトル）。どちらも SIL OFL 1.1
└─ <漫画タイトル>/                  ← 作品ごとのフォルダ（Git 管理外）
    ├─ manga.yaml                  ← 台本：あらすじ・キャラ・ページ・コマ・セリフ・生成プロンプト・採用版
    ├─ characters/                 ← キャラ設定画（顔の統一に使う）
    ├─ panels/                     ← コマ画像（p03_02.png。旧版は p03_02_v1.png のように残す）
    ├─ pages/                      ← 完成ページ（p01.jpg …）
    └─ output/                     ← cover.jpg / <タイトル>.epub / <タイトル>.pdf
```

スクリプトは uv のインラインスクリプトメタデータ（PEP 723）で依存ライブラリを宣言し、`uv run scripts/xxx.py` で実行する。
Python や ライブラリ（Pillow, google-genai, PyYAML 等）の個別インストールは不要。

### manga.yaml のページ部分（compose_page.py が読む形式）

```yaml
color: true
pages:
  - page: 1
    layout: 3_tall_right          # layouts/ のテンプレート名（--list-layouts で一覧）
    panels:                       # 読む順（右上から）。数はレイアウトのコマ数と同じ
      - id: p01_01
        image: panels/p01_01.png  # 採用版。省略時は panels/<id>.png。無ければ灰色の仮コマ
        focus: [0.45, 0.5]        # 切り抜くときに中心に来てほしい位置（画像に対する割合）
        zoom: 1.0                 # 1より大きいと寄る
        bubbles:
          - type: speech          # speech / shout / thought / whisper / narration
            text: "今日も
10時から
開いてますよ。"   # 
 で列を改行（読みやすい位置で明示する）
            pos: [0.3, 0.2]       # 吹き出しの中心（コマに対する割合）。narration は省略すると右上
            tail: [0.4, 0.5]      # しっぽが向く先（話し手の頭の方向）。顔に刺さらないよう長さは自動で制限
            size: 1.0             # 文字サイズの倍率
        sfx:
          - text: ツルッ
            pos: [0.8, 0.7]
            size: 2.5
            angle: -12
            color: "#141414"
            vertical: true
```

- 吹き出しはコマ枠からはみ出してよい。ナレーションはコマの内側に収める
- `--debug` で出る `pNN_debug.jpg` には、コマごとに 10% 刻みの目盛りが入る。位置の調整に使う
- `--info` で各コマの大きさと、生成時に指定すべきアスペクト比が分かる

## 5. モード

### 確認モード（デフォルト）

節目ごとにユーザーに確認し、費用のかかる画像生成の前に方向性を固める。

1. **入力確認** — 足りない項目があればまとめて1回だけ質問
2. **ネーム確認** — コマ割り・構図・セリフを文章で提示（この段階は無料）
3. **キャラ設定画確認** — キャラごとに設定画を生成して提示
4. **全ページ生成** — コマ生成 → ページ合成 → Claude が自己チェック → 問題のあるコマは自動リテイク
5. **完成ページを表示** — ビューアで自動的に開く
6. **修正ループ** — ユーザーの指示に応じて最小範囲をやり直す（6章）
7. **Kindle 書き出し** — ユーザーが「完成」「書き出して」と言ったら実行

### おまかせモード

「おまかせで」「全部まかせる」「最後まで作って」などの言葉で起動する。途中の確認をすべて省略し、書き出しまで一気に進める。

- ネームとキャラ設定画は Claude が自分で判断して確定する
- 入力が足りない項目も質問せず、Claude が補って決める（決めた内容は最後に報告する）
- 自己チェックで NG のコマは、コマごとに `config.yaml` の上限回数（例: 2回）までリテイクする
- 総生成枚数が `config.yaml` の上限（例: ページ数 × 8枚）を超えそうなら、そこで止めて相談する（費用の暴走防止）
- 最後に次の内容を報告し、完成ページと出力フォルダを開く
  - 決めた内容（補った設定、あらすじの解釈）
  - 生成枚数と概算費用
  - 気になった点（リテイク上限に達したコマなど）
- 完成後は、確認モードと同じように修正指示を受け付ける

## 6. 修正指示の扱い

`manga.yaml` を正本とし、指示の種類に応じてやり直す範囲を最小にする。

| 指示の例 | 処理 | 費用 |
|---|---|---|
| 「このセリフを変えて」「吹き出しの位置をずらして」 | `manga.yaml` を修正 → ページ再合成のみ | 0円・数秒 |
| 「3P2コマ目をもっと笑顔に」 | 該当コマのみ再生成 → 再合成 | 1枚分 |
| 「このコマ、前の方がよかった」 | 旧版（_v1 等）に戻して再合成 | 0円 |
| 「主人公の髪を短く」 | 設定画を再生成 → 影響するコマを列挙し、枚数と概算費用を提示 → 了承後に再生成 | 複数枚（事前確認） |
| 「4ページ目のコマ割りを変えて」 | レイアウト変更 → アスペクト比が変わるコマを再生成 | 数枚分 |
| 「ページを1枚足して」 | ネーム追加（確認）→ 生成 | 数枚分 |

- 旧版の画像は削除せず残し、`manga.yaml` で採用する版を指定する
- 複数枚の再生成が必要な指示は、おまかせモード中でも枚数と費用を提示してから実行する

## 7. Kindle 出力仕様

| 項目 | 仕様 |
|---|---|
| ページ画像 | 1600×2560px（縦横比 1:1.6）、JPEG、sRGB |
| 表紙 | 1600×2560px 以上の JPEG（タイトル文字入り） |
| EPUB | EPUB3 固定レイアウト、`page-progression-direction="rtl"`（右綴じ） |
| PDF | 確認・予備用 |
| 確認方法 | Kindle Previewer 3 で表示確認してから KDP にアップロード |

- カラーが既定のため、白黒 E-ink 端末ではグレースケール表示になる。そのため自己チェックでは、白黒にしたときに人物と背景が見分けられるか（明暗差が十分か）も確認する
- KDP 登録時に「AI 生成コンテンツ」の申告が必要（テキスト・画像とも）
- 出版前に KDP の最新の画像要件・EPUB 要件を確認する

## 8. 実装ステップ

| # | 内容 | 担当 | 完了条件 |
|---|---|---|---|
| 0 | GitHub 非公開リポジトリ作成、`.gitignore` / `.env` 準備 | 本人 | リポジトリ作成済み、API キー設定済み |
| 1 | `gen_image.py` を作り、試験生成（キャラ設定画 → 別構図で3コマ）で顔が揃うか・画風の限界を検証 | Claude | 同一キャラと判別できる品質 |
| 2 | `compose_page.py`（レイアウト、吹き出し、縦書き、約物の回転処理）を作る | Claude | サンプル2ページが自然に読める |
| 3 | `export_kindle.py` を作り、Kindle Previewer で表示確認 | Claude＋本人 | 右綴じで正しくめくれる |
| 4 | `SKILL.md`（2モード・修正ルール）、`styles.md`、`config.yaml` を作る | Claude | － |
| 5 | Windows で4ページ短編を両モードで通しテスト | 本人 | 修正指示ループが回る |
| 6 | Mac セットアップ、パートナーによる試用 | 本人＋パートナー | パートナーが1人で1作品作れる |

## 9. 費用の目安

- 画像1枚あたり: `gemini-3.1-flash-image` 2K で $0.101（約15円）。高品質の `gemini-3-pro-image` は $0.134、安価な `gemini-3.1-flash-lite-image` は 1K のみで $0.034（2026-09 時点。無料枠なし）
- 例: 20ページ × 5コマ ＋ 設定画・表紙・リテイク ≒ 150枚 → 数百円〜3,000円程度
- Claude Code の利用料（サブスクリプション）は別途かかる

## 10. セットアップ手順

### 10.1 共通（アカウント）

- **Claude の有料プラン（Pro 以上）**: Claude Code の利用に必要。**1人1アカウント**（パートナー用も必要）
- **Google AI Studio の API キー**: 本人のものを使用（画像モデルの利用には支払い設定が必要な場合がある）
- **GitHub**: 本人のアカウントで非公開リポジトリを作成

### 10.2 Windows 11（本PC）

導入済みのもの: Python 3.14、Node.js 20、Git 2.54

追加でインストールするもの:

1. **uv**（Python 実行ツール。PowerShell で実行）
   ```powershell
   powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
   ```
2. **Kindle Previewer 3**（Amazon 公式・無料）: EPUB の表示確認用
3. `.env` を作成して API キーを記入

作業フォルダ: `C:\Users\temoy\Documents\dev\MangaKun`（Git と同期の衝突を避けるため OneDrive 配下には置かない）

### 10.3 MacBook Air（パートナーのPC）— 本人が実施

#### Mac 操作の基本

- **ターミナル**（コマンドを打つアプリ）を開く: `⌘ command` + `スペース` → 「ターミナル」と入力 → `return`
- **貼り付け**: `⌘ command` + `V`（Windows の Ctrl+V）
- コマンドは手で打たずにコピー＆ペーストする。日本語入力がオンだと失敗するため、キーボードの **「英数」キー** を押してから貼り付ける
- パスワードを聞かれたら Mac のログインパスワードを入力する。**入力しても画面に何も表示されないのは正常**。そのまま `return` を押す
- ファイルの中身を見る: Finder でファイルを選んで `スペース` キー（クイックルック）

#### ① Git（Xcode コマンドラインツール）をインストール

```bash
xcode-select --install
```

表示されたダイアログで **「インストール」** → 利用規約に **「同意する」**。10〜20分ほどかかる。
完了後、`git --version` と入力してバージョンが表示されれば成功。

#### ② uv をインストール

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

完了後、ターミナルを一度終了（`⌘ Q`）して開き直す。

#### ③ Claude Code をインストール

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

続けて `claude` と入力して `return` を押すとブラウザが開くので、**パートナーのアカウント**でログインする。

#### ④ リポジトリを取得

非公開リポジトリなので、読み取り専用の認証を用意する。
GitHub の Fine-grained personal access token を作成し、対象はこのリポジトリのみ、権限は Contents: Read-only とする。

```bash
cd ~
git clone https://github.com/<ユーザー名>/MangaKun.git
```

ユーザー名と、パスワードの代わりにトークンを聞かれるので、両方入力する。
トークンは Mac のキーチェーンに保存されるため、次回からは聞かれない。

保存場所はホームフォルダ直下（`~/MangaKun`）にする。「書類」フォルダは iCloud 同期の対象になっていることがあり、Git と相性が悪いため避ける。

#### ⑤ API キーを配置

```bash
cd ~/MangaKun
cp .env.example .env
open -e .env
```

テキストエディットが開くので、API キーを記入して保存（`⌘ S`）する。

#### ⑥ 普段使いは Claude デスクトップアプリ

1. claude.ai/download から Mac 版をダウンロードし、開いた画面で Claude アイコンを「アプリケーション」フォルダへドラッグ
2. 初回起動で「インターネットからダウンロードされたアプリケーションです」と出たら **「開く」**
3. パートナーのアカウントでログイン
4. **Code** タブで `MangaKun` フォルダを選択
5. 「以下の漫画を作って」と話しかければ使える（ターミナル操作は不要）

#### ⑦ よく出る確認ダイアログ

- 「"Claude"（または"ターミナル"）がファイルにアクセスしようとしています」→ **「許可」**
- ファイル編集やコマンド実行の許可を Claude から求められたら → 基本的に **「許可」**（本人が事前に `.claude/settings.json` で必要な許可を設定しておき、確認の回数を減らす）

#### ⑧ スキルの更新方法（パートナー向け）

本人が GitHub に更新を push した後、パートナーは Claude に **「スキルを更新して」** と言うだけ。
Claude が `git pull` を実行する（`SKILL.md` にこの手順を書いておく）。

## 11. 未確定・実装時に確認すること

- ~~Gemini 画像モデルの最新のモデル名・料金・参照画像の上限枚数・対応アスペクト比~~ → 確認済み（2026-09-23）
  - モデル: `gemini-3.1-flash-image`（標準）/ `gemini-3-pro-image`（高品質）/ `gemini-3.1-flash-lite-image`（安価・1K のみ）
  - 参照画像: 3.1 Flash はキャラ画像4枚まで（物体画像は10枚まで）、3 Pro はキャラ画像5枚まで
  - アスペクト比: 1:1, 3:2, 2:3, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9（コマの形に合わせて近いものを選び、合成時に切り抜く）
  - 2K の実寸: 16:9 → 2752×1536、2:3 → 1696×2528、4:3 → 2400×1792
  - 試験結果: 設定画1枚を参照させるだけで、アップ・全身・2人の会話の3コマとも同一人物と判別できる品質だった
- ~~漫画向けフォントのライセンス（同梱・商用出版の可否）~~ → 確認済み: 源暎アンチック v6.0a・Dela Gothic One とも SIL OFL 1.1 で同梱・商用出版可
- KDP の最新の画像・EPUB 要件、AI 生成コンテンツの申告方法
- Claude デスクトップアプリ（Code タブ）でプロジェクトスキルが読み込まれることの確認
